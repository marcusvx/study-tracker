<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Local dev only: lets the app reach the backend over plain HTTP
         when running against localhost/10.0.2.2 (Android emulator alias
         for the host machine). Production traffic still requires HTTPS. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
